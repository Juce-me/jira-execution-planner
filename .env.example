# Jira Configuration
# Your Jira instance URL (e.g., https://your-company.atlassian.net)
JIRA_URL=https://your-company.atlassian.net

# Your Jira email
JIRA_EMAIL=your-email@company.com

# Your Jira API token (get it from https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_TOKEN=your-api-token-here

# Optional: base JQL query for task filtering (projects & teams are now configured via the UI)
# Only needed for extra filters like status/type. Defaults to a simple project query if unset.
# JQL_QUERY=status != Killed AND type = Story ORDER BY priority DESC

# Optional: Jira Board ID for faster sprint fetching
# If not set, sprints will be fetched from issues (slower but works without board ID)
JIRA_BOARD_ID=

# Optional: port for the Flask server (defaults to 5050 if unset)
SERVER_PORT=5050

# Optional: enable Flask debug mode (auto-reload on code changes, detailed errors)
# Set to 'true' for development, 'false' for production (defaults to 'false')
DEBUG_MODE=false

# Optional: future access-control flag for configuration tabs (no UI gating enforced yet)
# Defaults to true (settings intended to be admin-only, except group config in future SSO mode)
# SETTINGS_ADMIN_ONLY=true

# Optional: team IDs used to scope the "Empty Epic" alert check (matches "Team[Team]" values in JQL)
# Example: EPIC_EMPTY_TEAM_IDS=TEAM_ID_A,TEAM_ID_B
# EPIC_EMPTY_TEAM_IDS=

# Optional: Jira project key/name that stores team capacity entries (Team info <SPRINT> - <TEAM>)
# Example: CAPACITY_PROJECT=TEAM CAPACITY
# CAPACITY_PROJECT=

# Optional: Jira field name or ID used for team capacity values
# Example: CAPACITY_FIELD_NAME=Team capacity[Number]
# CAPACITY_FIELD_ID=customfield_12345

# Optional: excluded statuses for the "Empty Epic" alert panel (comma-separated)
# EPIC_EMPTY_EXCLUDED_STATUSES=Killed,Done,Incomplete,In Progress

# Optional: extra scan to find stories under epics in a sprint that are missing planning fields (sprint/SP/team)
# MISSING_INFO_TEAM_IDS=TEAM_ID_A,TEAM_ID_B

# Priority weights for weighted delivery stats
STATS_PRIORITY_WEIGHTS=Blocker:0.40,Critical:0.30,Major:0.20,Minor:0.06,Low:0.03,Trivial:0.01
